git config --global user.email "majunseob@gmail.com"
git config --global user.name "마준섭"

git init
git add git.txt
git commit -m 'git.txt 파일생성'

작업폴더 --[git add]-> staging area --[git commit]-> repository

git add git.txt git2.txt
git add .

git status
git log --all --oneline

git diff
jk q

git difftool
hjkl :q :qa

git difftool 커밋아이디

Git History, GitLens, Git Graph 등 더 좋은것들이 있다.